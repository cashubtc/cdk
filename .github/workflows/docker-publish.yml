name: Publish Docker Images

on:
  release:
    types: [published]
  workflow_dispatch:
    inputs:
      tag:
        description: 'Tag to build and publish'
        required: true

env:
  REGISTRY: docker.io
  IMAGE_NAME: cashubtc/mintd

jobs:
  build:
    strategy:
      fail-fast: false
      matrix:
        arch:
          - name: x86_64
            runner: self-hosted
            docker_platform: linux/amd64
            docker_arch: amd64
            install_nix: false
          - name: aarch64
            runner: ubuntu-24.04-arm
            docker_platform: linux/arm64
            docker_arch: arm64
            install_nix: true
        variant:
          - name: standard
            nix_target: cdk-mintd-static
            tag_suffix: ""
          - name: ldk-node
            nix_target: cdk-mintd-ldk-static
            tag_suffix: "-ldk-node"
    runs-on: ${{ matrix.arch.runner }}
    timeout-minutes: 120
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          ref: ${{ github.event.inputs.tag || github.ref }}

      - name: Install Nix
        if: ${{ matrix.arch.install_nix }}
        uses: DeterminateSystems/nix-installer-action@v14

      - name: Set up Cachix
        uses: cachix/cachix-action@v16
        with:
          name: cashudevkit
          authToken: ${{ secrets.CACHIX_AUTH_TOKEN }}
          useDaemon: false
          installCommand: ${{ matrix.arch.install_nix && '' || 'nix profile install nixpkgs#cachix' }}
        continue-on-error: true

      - name: Build static binary
        run: |
          nix build .#${{ matrix.variant.nix_target }} -L
          mkdir -p ./docker-build
          cp -f ./result/bin/* ./docker-build/cdk-mintd
          cp Dockerfile.static ./docker-build/Dockerfile

      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
        with:
          driver-opts: network=host

      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Determine tag
        id: tag
        env:
          EVENT_NAME: ${{ github.event_name }}
          RELEASE_TAG: ${{ github.event.release.tag_name }}
          INPUT_TAG: ${{ github.event.inputs.tag }}
        run: |
          if [[ "$EVENT_NAME" == "release" ]]; then
            RAW_VERSION="$RELEASE_TAG"
          else
            RAW_VERSION="$INPUT_TAG"
          fi
          echo "raw_version=${RAW_VERSION}" >> "$GITHUB_OUTPUT"
          echo "version=${RAW_VERSION#v}" >> "$GITHUB_OUTPUT"

      - name: Build and push Docker image
        uses: docker/build-push-action@v5
        with:
          context: ./docker-build
          push: true
          platforms: ${{ matrix.arch.docker_platform }}
          tags: ${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${{ steps.tag.outputs.version }}${{ matrix.variant.tag_suffix }}-${{ matrix.arch.docker_arch }}
          cache-from: type=gha
          cache-to: type=gha,mode=max

  manifest:
    needs: build
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write
    strategy:
      fail-fast: false
      matrix:
        variant:
          - name: standard
            tag_suffix: ""
          - name: ldk-node
            tag_suffix: "-ldk-node"

    steps:
      - name: Login to Docker Hub
        uses: docker/login-action@v3
        with:
          username: ${{ secrets.DOCKERHUB_USERNAME }}
          password: ${{ secrets.DOCKERHUB_TOKEN }}

      - name: Determine tag
        id: tag
        env:
          EVENT_NAME: ${{ github.event_name }}
          RELEASE_TAG: ${{ github.event.release.tag_name }}
          INPUT_TAG: ${{ github.event.inputs.tag }}
          IS_STABLE: ${{ !github.event.release.prerelease && !contains(github.event.release.tag_name, 'rc') }}
        run: |
          if [[ "$EVENT_NAME" == "release" ]]; then
            RAW_VERSION="$RELEASE_TAG"
            echo "is_stable=$IS_STABLE" >> "$GITHUB_OUTPUT"
          else
            RAW_VERSION="$INPUT_TAG"
            echo "is_stable=false" >> "$GITHUB_OUTPUT"
          fi
          echo "raw_version=${RAW_VERSION}" >> "$GITHUB_OUTPUT"
          echo "version=${RAW_VERSION#v}" >> "$GITHUB_OUTPUT"

      - name: Extract semver components
        id: semver
        env:
          VERSION_INPUT: ${{ steps.tag.outputs.version }}
        run: |
          VERSION="$VERSION_INPUT"
          # Strip leading 'v' if present
          VERSION="${VERSION#v}"
          MAJOR="${VERSION%%.*}"
          MINOR="${VERSION#*.}"
          MINOR="${MINOR%%.*}"
          echo "major_minor=${MAJOR}.${MINOR}" >> "$GITHUB_OUTPUT"

      - name: Create versioned manifest
        env:
          VERSION: ${{ steps.tag.outputs.version }}
        run: |
          docker buildx imagetools create \
            -t "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${VERSION}${{ matrix.variant.tag_suffix }}" \
            "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${VERSION}${{ matrix.variant.tag_suffix }}-amd64" \
            "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${VERSION}${{ matrix.variant.tag_suffix }}-arm64"

      - name: Create major.minor manifest
        if: ${{ steps.tag.outputs.is_stable == 'true' }}
        env:
          MAJOR_MINOR: ${{ steps.semver.outputs.major_minor }}
          VERSION: ${{ steps.tag.outputs.version }}
        run: |
          docker buildx imagetools create \
            -t "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${MAJOR_MINOR}${{ matrix.variant.tag_suffix }}" \
            "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${VERSION}${{ matrix.variant.tag_suffix }}-amd64" \
            "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${VERSION}${{ matrix.variant.tag_suffix }}-arm64"

      - name: Create latest manifest
        if: ${{ steps.tag.outputs.is_stable == 'true' }}
        env:
          VERSION: ${{ steps.tag.outputs.version }}
        run: |
          docker buildx imagetools create \
            -t "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:latest${{ matrix.variant.tag_suffix }}" \
            "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${VERSION}${{ matrix.variant.tag_suffix }}-amd64" \
            "${{ env.REGISTRY }}/${{ env.IMAGE_NAME }}:${VERSION}${{ matrix.variant.tag_suffix }}-arm64"
